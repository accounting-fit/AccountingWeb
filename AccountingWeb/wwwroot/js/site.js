$(document).ready(function () {
    //$('.datepicker').datepicker({
    //    format: "dd-mm-yyyy",
    //    autoclose: true,
    //    todayHighlight: true,
    //    todayBtn: "linked",
    //    clearBtn: true,
    //});
    $('select').select2({
        //theme: 'classic'
    });

    
});

function DateShow(el) {
    el.focus();
}